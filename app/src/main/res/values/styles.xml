<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Custom Toolbar Style -->
    <style name="ToolbarStyle" parent="Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:titleTextColor">@android:color/black</item>
        <item name="android:buttonBarButtonStyle">@style/Base.Theme.EatGreen</item>
    </style>

    <!-- App Theme -->
    <style name="Theme.EatGreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:actionBarStyle">@style/ToolbarStyle</item>
    </style>

</resources>



